flask>=3.0.0
gunicorn>=20.1.0
pymupdf>=1.23.0
Pillow>=10.0.0
reportlab>=4.0.0
# Windows only (MS Office COM automation) — opsional, fallback ke LibreOffice jika tidak ada
pywin32>=306; sys_platform == "win32"
